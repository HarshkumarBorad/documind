"""DocuMind MCP server package.

The server object lives in `mcp_server.server` — import it from there:

    from mcp_server.server import mcp

Kept empty here so `python -m mcp_server.server` doesn't trigger a
"module found in sys.modules" RuntimeWarning from the runpy machinery.
"""
